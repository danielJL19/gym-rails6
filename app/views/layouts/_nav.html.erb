<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white fw-bold" href="#">Gym Municipal</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link active text-white" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="#">Quienes Somos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="#">Contactanos</a>
        </li>
        <li class="nav-item">
          <% unless user_signed_in?  %>
            <%= link_to users_sign_in_path, style:"color:white" do %>
              <i class="bi bi-person" style="font-size:30px;"></i>
            <% end %>
          <% else%>
            <%= link_to users_sign_out_path, method: :delete, style:"color:white" do %>
              <i class="bi bi-box-arrow-left" style="font-size:30px;"></i>
            <% end %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
